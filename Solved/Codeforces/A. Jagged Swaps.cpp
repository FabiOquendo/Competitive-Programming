
#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
	
	int t;
	cin >> t;
 
	while (t > 0) {
		int n;
		cin >> n;
		
		int a, a1;
		cin >> a1;
		for (int i = 1; i < n; i++)
			cin >> a;
			
		cout << (a1 == 1 ? "YES" : "NO") << "\n";
	    
		t--;
	}
	
	return 0;
}